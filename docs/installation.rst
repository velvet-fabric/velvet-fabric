============
Installation
============

At the command line::

    $ easy_install velvet_fabric

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv velvet_fabric
    $ pip install velvet_fabric